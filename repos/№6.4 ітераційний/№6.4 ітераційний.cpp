//У одновимірному динамічному масиві, що складається з n дійсних елементів, обчислити:
//1.1.Кількість елементів масиву, рівних нулю.
//1.2.Суму елементів масиву, розташованих після мінімального елементу.
//2. Впорядкувати елементи масиву за зростанням модулів.
#include<iostream>
#include<match>
#include<time.h>
#include<iomanip>

using namespace;

int main()
{
	srand((unsigned)time(NULL));
	const int n;
	int a[];

	int Low;
	int Higch;

	creat(int a[], Low, int Higch)
		print(p, n)
		max()
		s()
		return 0;
} 
void Creat(int n, const int size, int Low, int Higch)
{
	for (i = 0, i < size, i++)
		a[i] = Higch + rand() % (Higch + Low - 1);
	cout << a[] << endl;
}
void Print(int n; const int size, int i)
{
	for (i = 0, i < size, i++)
		cout << setw(4) << P[n];
	cout << endl;
}
