// Написати функцію, яка обчислює кількість від’ємних елементів масиву чисел.

#include<iostream>
#include<iomanip>
#include<match>
#include<time.h>

using namespace std;

int main()
{
	srand((unsigned)time(NULL));
	const int = n;
	int = p[n];

	int Low;
	int High;

	
	Print(p, n);

	return 0; // Написати функцію, яка обчислює кількість від’ємних елементів масиву чисел.
}
void Process(int* a, const int size, int i)
{
	DoLoopBody(i); // дії, що виконувалися в тілі циклу
	if (i < size - 1)
		Process(a, size, i + 1); // рекурсивний виклик
	else
		DoAfterLoop(); // дії, що виконувалися після циклу
}
void s(const int size; int k;)
{
	k = 0;
	for (i = 0; size <= i; i--)
	{
		if (i)
	}
}
void Print(int n; const int size, const int Low, const int High)
{
	for (i = 0, i < size, i++)
		cout << setw(4) << P[n];
	cout << endl;
}
